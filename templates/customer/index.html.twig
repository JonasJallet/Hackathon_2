{% extends 'base.html.twig' %}

{% block title %}Customer index
{% endblock %}

{% block body %}
	<h3 class="text-center mt-5 mb-5">Liste des utilisateurs</h3>

	<table class="table">
		<thead>
			<tr>
				<th>Prénom</th>
				<th>Nom</th>
				<th>N° CMI</th>
				<th>Téléphone</th>
				<th>Addresse</th>
				<th>Code Postal</th>
				<th>Ville</th>
				<th>Actions</th>
			</tr>
		</thead>
		<tbody>
			{% for customer in customers %}
				<tr>
					<td>{{ customer.firstName }}</td>
					<td>{{ customer.lastName }}</td>
					<td>{{ customer.disabilityCard }}</td>
					<td>{{ customer.phone }}</td>
					<td>{{ customer.address }}</td>
					<td>{{ customer.zipcode }}</td>
					<td>{{ customer.city }}</td>
					<td>
						<a href="{{ path('app_customer_show', {'id': customer.id}) }}"><img class="" src="{{ asset('build/images/show.png') }}" alt="" width="35" heigh="35"></a>
						<a href="{{ path('app_customer_edit', {'id': customer.id}) }}"><img class="" src="{{ asset('build/images/edit.png') }}" alt="" width="35" heigh="35"></a>
					</td>
				</tr>
			{% else %}
				<tr>
					<td colspan="9">Aucun utilisateur trouvé</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
